collectd_exporter
-----------------

Based on https://github.com/prometheus/collectd_exporter/blob/master/Dockerfile

CLI
---
```
% docker run -it --rm docker.io/polymathrobotics/collectd_exporter --help
usage: collectd_exporter [<flags>]

    Flags:
    -h, --help                   Show
    context-sensitive
    help
    (also
    try
    --help-long
    and
    --help-man).
    --web.listen-address=":9103"
    Address
    on
    which
    to
    expose
    metrics
    and
    web
    interface.
    --collectd.listen-address=""
    Network
    address
    on
    which
    to
    accept
    collectd
    binary
    network
    packets,
    e.g.
    ":25826".
    --collectd.udp-buffer=0  Size
    of
    the
    receive
    buffer
    of
    the
    socket
    used
    by
    collectd
    binary
    protocol
    receiver.
    --collectd.auth-file=""  File
    mapping
    user
    names
    to
    pre-shared
    keys
    (passwords).
    --collectd.security-level="None"
    Minimum
    required
    security
    level
    for
    accepted
    packets.
    Must
    be
    one
    of
    "None",
    "Sign"
    and
    "Encrypt".
    --collectd.typesdb-file=""
    Collectd
    types.db
    file
    for
    datasource
    names
    mapping.
    Needed
    only
    if
    using
    a
    binary
    network
    protocol.
    --web.telemetry-path="/metrics"
    Path
    under
    which
    to
    expose
    Prometheus
    metrics.
    --web.collectd-push-path="/collectd-post"
    Path
    under
    which
    to
    accept
    POST
    requests
    from
    collectd.
    --log.level=info         Only
    log
    messages
    with
    the
    given
    severity
    or
    above.
    One
    of:
    [debug,
    info,
    warn,
    error]
    --log.format=logfmt      Output
    format
    of
    log
    messages.
    One
    of:
    [logfmt,
    json]
```